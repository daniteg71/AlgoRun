# Core NLP
# Nota: su Python 3.9 installare spacy<3.8 (es. 3.7.2) — le versioni 3.8.x
# richiedono thinc>=8.3.12 che a sua volta vuole Python 3.10+. Poi:
#   python -m spacy download en_core_web_sm
spacy>=3.7,<4.0

# Ontology / RDF / SHACL
rdflib>=7.0
pyshacl>=0.26

# Vector scoring + ML classico (scorer e validator LEGGERO)
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4

# Fuzzy matching per il join con la playlist personale
rapidfuzz>=3.6

# Testing
pytest>=8.0

# Il benchmark Transformer (DistilBERT) e' opzionale: vedi requirements-bench.txt.
